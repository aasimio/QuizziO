/// Hive box name constants for local storage
class HiveBoxes {
  HiveBoxes._();

  /// Box for storing quiz data (QuizModel)
  static const String quizzes = 'quizzes';

  /// Box for storing scan results (ScanResultModel)
  static const String scanResults = 'scan_results';
}
